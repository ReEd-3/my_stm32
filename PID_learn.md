# PID控制原理

## PID算法的组成

- **比例（Proportional）**：比例控制考虑**当前的控制偏差**，通过*比例系数(Kp*)来调整控制输出。比例控制能够快速响应偏差，但无法消除稳态误差。

- **积分（Integral）**：积分控制关注**过去累积的偏差**，通过*积分系数(Ki*)来调整控制输出。积分控制有助于消除稳态误差，但可能导致系统超调和振荡。

- **微分（Differential）**：微分控制预测**未来的偏差变化趋势**，通过*微分系数(Kd*)来调整控制输出。微分控制能够减少超调和改善系统的动态响应。


## 控制流程

```text

>                                                            +
>                                      +------>|     Kp    |------>+
>           +   +---+                  |                     +     |                      +------------+
>    设定值---->|    |--->err（误差）---|------>| Ki * ∫ dt |------>+-->CO（PID输出值）--->| S域传递函数  |------+--->
>               +---+                  |                     +     |                      +------------+       |
>                 ^                    +------>| Kd * d/dt |------>+                                           |
>           FB  - |                                                                                            |
>                 +--------------------------------------------------------------------------------------------+

```

## 传递函数

1. 例如输入值为**X（t）**，输出值为**Y（t）**（其中t为时域），则传递函数**G（s）**为**Y（s） / X（s）**  
这个过程中要把X，Y转换成s域上的函数

2. 对X，Y进行转换用到**拉普拉斯变换**，遵循  
    `Ⅼ{X(t)} = X(s)`  
    `Ⅼ{X'(t)} = s X(s)`  
    `Ⅼ{X''(t)} = s² X(s)`  
    `Ⅼ{∫[X(t)]} = s⁻¹ X(s)`  

3. 列出关于X，Y的微分方程，进行**拉普拉斯变换**之后求解数**G（s）** 得到传递函数

4. 传递函数的标准形式为
`G(s) = K*(s/z₁ + 1)(s/z₂ + 1)……(s/z_m + 1)(s^-v)(s/p₁ + 1)⁻¹(s/p₂ + 1)⁻¹……(s/p_n + 1)⁻¹`


## 调参

1. P,I,D参数不合适时可能会出现的影响:
    以对输出的波形图的影响为例
    |参数         |过大                 |过小                                                         |
    |---          |---                  |---                                                         |
    |**Kp**       |出现震荡甚至发散      |使系统响应迟缓                                               |
    |**Ki**       |在设定值附近缓慢震荡，有可能发散|会使稳态误差无法消除，导致最终真实值和设定值之间误差较大|
    |**Kd**       |对噪声敏感导致剧烈抖动，使系统做出响应的速度变慢|使系统在到达设定值之前有明显振荡|

2. 波特图 
    波特图横轴为**ω**，单位**rad/s**，刻度为**lg对数分布**，例如0.1，1，10这三个刻度之间的间隔是相等的 
    纵轴为**幅度**，单位**dB**，刻度为**lg对数分布** 

    对于`G(s) = K*(s/z₁ + 1)(s/z₂ + 1)……(s/z_m + 1)(s^-v)(s/p₁ + 1)⁻¹(s/p₂ + 1)⁻¹……(s/p_n + 1)⁻¹`  
    - 其中`K`代表增益，`v`代表型别，波特图的基准型（一条直线）由增益和型别确定  
        - **v**与波特图**基准型斜率**之间的关系是  
            `v=0,斜率   0 dB/dec`  
            `v=1,斜率 -20 dB/dec`  
            `v=2,斜率 -40 dB/dec`  
        这里`1dec`表示横轴上0.1到1之间的距离  

        - **K**与波特图**增益**之间的关系是  
        `(1 , 20lgK)`  
        波特图经过这个点
    - G(s)的零点为**z₁、z₂、……、z_m**，极点为**p₁、p₂、……、p_n**  
    在横轴找到这些点，基准型**遇到零点**时斜率\+**20dB**，**遇到极点**时斜率\-**20dB**  
    最终得出的图像是**幅频特性曲线**

## 实操

1. 对PID功能进行**模块化封装**，首先在**Core**文件下的**Inc**和**Src**两个文件中新建c源文件和头文件，例如**pid.c**和**pid.h**  

2. 在头文件中用`#ifndef PID_H`和`#define PID_H`定义PID_H的宏并用`#endif`表示结束，在源文件中使用`#include "pid.h"`  

3. 在**pid.h**中使用`#include "main.h"`，并且声明PID结构体和函数

4. 在**pid.c**中分别编写进行**PID初始化**和**进行PID计算**的函数